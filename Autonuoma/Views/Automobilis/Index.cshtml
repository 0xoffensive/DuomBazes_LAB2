@model IEnumerable<Org.Ktu.Isk.P175B602.Autonuoma.Models.Automobilis.AutomobilisL>

@* this will also be used as page title in main layout *@
@{ ViewData["title"] = "Automobiliai"; }

@* content header *@
<div class="main-content-header">
	<span class="title">@ViewData["title"]</span>
	<span class="actions">
		<a href="@Url.Action("Create")" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Sukurti naują</a>
	</span>
</div>

@* content itself *@
<div class="main-content-lst">
	<table class="entity-list">
		<thead>
			<tr>
				<th>
					@Html.DisplayNameFor(m => m.Vin)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.ValstybinisNr)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Modelis)
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model) {
			<tr>
				<td>
					@Html.DisplayFor(m => item.Vin)
				</td>
				<td>
					@Html.DisplayFor(m => item.ValstybinisNr)
				</td>
				<td>
					@Html.DisplayFor(m => item.Gamintojas) @Html.DisplayFor(m => item.Modelis)
				</td>
				<td>
					<a href="@Url.Action("Edit", new { vin = item.Vin })" class="btn btn-primary"><i class="fa-solid fa-pen-to-square"></i> Redaguoti</a>
					<a href="@Url.Action("Delete", new { vin = item.Vin })" class="btn btn-danger"><i class="fa-solid fa-trash"></i> Šalinti</a>
				</td>
			</tr>
			}
		</tbody>
	</table>
</div>
